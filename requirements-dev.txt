# TF-0005 — Dependencias de desarrollo / test (NO se instalan en la imagen Docker)
# Entorno verificado: Python 3.8.10
#
# Instalar con:  pip install -r requirements.txt -r requirements-dev.txt
#
# pytest 8.3.x es la última serie compatible con Python 3.8 (8.4+ exige >= 3.9).
# pytest-cov 5.0.x es la última compatible con Python 3.8 (6.x exige >= 3.9);
# coverage 7.6.1 es la última que soporta 3.8 (7.7+ lo abandona).
# Transitivas fijadas con == para builds reproducibles, mismo criterio que TF-0002.

pytest==8.3.5
pytest-cov==5.0.0

# --- Transitivas de pytest / pytest-cov (pinned) ---
pluggy==1.5.0
iniconfig==2.1.0
packaging==26.2
exceptiongroup==1.3.1
tomli==2.4.1
typing-extensions==4.13.2
coverage==7.6.1
