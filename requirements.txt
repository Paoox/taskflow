# TF-0002 — Dependencias de Taskflow
# Entorno verificado: Python 3.12 (imagen python:3.12-slim; ver TF-0011).
#
# Dependencia directa (import real en app.py):
#   - Flask
# El resto de paquetes son dependencias transitivas de Flask, fijadas
# explicitamente para garantizar builds reproducibles.
#
# TF-0011: se eliminaron importlib-metadata y zipp. Flask solo las requiere en
# Python < 3.10 (marker `python_version < "3.10"`); en 3.12 no las usa nadie
# (`pip show importlib-metadata` -> Required-by vacío).

Flask==3.0.3

# --- Transitivas de Flask (pinned para reproducibilidad) ---
Werkzeug==3.0.6
Jinja2==3.1.6
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.1.8
blinker==1.8.2
