# TF-0001 — Recuperar funcionalidad básica de la aplicación / interfaz inicial

## Estado

DONE

## Tipo

No etiquetado formalmente en su momento. Naturaleza: recuperación de funcionalidad + interfaz inicial (BUG / UI).

## Prioridad

No registrada.

## Objetivo

Recuperar la funcionalidad básica de la aplicación Flask y establecer la interfaz
inicial del prototipo (HTML + CSS + Jinja2).

## Contexto

Documento reconstruido retroactivamente como parte de TF-0004.

El trabajo se realizó en una sesión previa de Claude
(`session_01T6vf1XnY36oGQvf22wUD6V`) que no está disponible. Toda la información
de este documento proviene exclusivamente del historial de Git; no se añade nada
no verificable.

## Cambios realizados

- `app.py`: las vistas `index` y `crear_tarea_web` pasan los datos a las
  plantillas como diccionarios (`[t.to_dict() for t in ...]` /
  `[p.to_dict() for p in ...]`) en lugar de pasar los objetos `Tarea` / `Proyecto`
  directamente.
- Se añaden las plantillas Jinja2:
  - `templates/base.html` (24 líneas)
  - `templates/index.html` (52 líneas)
  - `templates/formulario_tarea.html` (50 líneas)
- Se añade la hoja de estilos `static/style.css` (223 líneas).

## Archivos afectados

| Archivo | Cambio |
|---|---|
| `app.py` | modificado (+5 / −4) |
| `static/style.css` | nuevo (223 líneas) |
| `templates/base.html` | nuevo (24 líneas) |
| `templates/index.html` | nuevo (52 líneas) |
| `templates/formulario_tarea.html` | nuevo (50 líneas) |

Total del commit: 5 archivos, +354 / −4.

## Pruebas

No consta registro de pruebas para este ticket.

Verificación indirecta: el ticket posterior TF-0002 confirmó `GET /` y
`GET /crear` = 200 sobre esta misma base de código.

## Criterios de aceptación

No se formalizaron criterios de aceptación en su momento.

## Decisiones y riesgos

No consta registro de decisiones ni riesgos.

## Commit

- **SHA:** `110d15fee8d70c9ba98bcd9222758d6aa4e5d6d8` (`110d15f`)
- **Mensaje:** `TF-0001: recuperar funcionalidad básica de la app`

Nota: el commit inmediatamente posterior, `7d585bd` (`interfaz basica`), añadió
`.idea/` a `.gitignore`. No lleva prefijo de ticket, pero es contiguo a este
trabajo y no corresponde a ningún otro ticket.

## Notas

Documento retroactivo. Si aparece la transcripción de la sesión original, puede
completarse la sección de pruebas y decisiones sin alterar lo ya registrado.
