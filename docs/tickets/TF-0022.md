# TF-0022 — Persistencia de ejecuciones: tabla `acciones`

## Estado

PLANNED — diseño aprobado por Pao el 2026-08-29. Secuencia de andamiaje:
TF-0019 → TF-0020 → TF-0021 → **TF-0022**. Implementación pendiente; no se inicia
hasta que TF-0019, TF-0020 y TF-0021 estén en `main` y en verde, y con checkpoint
de diseño previo (esquema y API del repositorio).

## Tipo

ARCH

## Prioridad

P2

## Objetivo

Registro persistente y consultable de cada ejecución relevante (humana o de
agente) asociada a un ticket, para materializar la trazabilidad de `CLAUDE.md`
§28. **Una sola tabla nueva**, con el patrón de persistencia actual (SQLite,
`CREATE TABLE IF NOT EXISTS` dentro de `crear_tablas()`, sin ORM ni migraciones).

La tabla `acciones` es **infraestructura de trazabilidad, no parte del dominio de
tareas/proyectos**.

## Contexto

Promoción de **BL-17** (origen: análisis de arquitectura para agentes,
2026-08-29).

Estado actual: `crear_tablas()` en `src/database.py` crea `proyectos` y `tareas`
(con FK `tareas.proyecto_id → proyectos.id`) y siembra el proyecto `id=0`.
`get_connection()` ejecuta `PRAGMA foreign_keys = ON` (TF-0015). El acceso a datos
sigue el patrón "una conexión por operación" (`DBManager`). No hay ningún registro
de ejecuciones/acciones.

El `ticket` que referenciará `acciones` es un identificador textual `TF-XXXX` del
sistema de tickets de `docs/`, **no** una fila de `tareas` ni de `proyectos`.

## Alcance

- Extender `crear_tablas()` en `src/database.py` con:

  ```sql
  CREATE TABLE IF NOT EXISTS acciones (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    ticket         TEXT,
    actor          TEXT NOT NULL,      -- p. ej. 'humano:pao' | 'agente:documentador'
    tipo           TEXT NOT NULL,      -- p. ej. 'generar_doc_ticket'
    entrada        TEXT,               -- JSON (string)
    resultado      TEXT,               -- JSON (string)
    estado         TEXT NOT NULL,      -- EN_CURSO | COMPLETADA | FALLIDA
    creado_en      TEXT NOT NULL,
    actualizado_en TEXT
  )
  ```

  Sin FK (el `ticket` no referencia `tareas`/`proyectos`). Sin índices (hasta que
  haya volumen real, §30).
- Nuevo `src/repositorios/__init__.py` y `src/repositorios/acciones.py` con
  `RepositorioAcciones`:
  - `registrar(ticket, actor, tipo, entrada) -> int` — crea la fila en `EN_CURSO`
    con `creado_en` = ahora; devuelve el `id`.
  - `marcar(id, estado, resultado=None) -> bool` — actualiza `estado`,
    `resultado` y `actualizado_en`; devuelve `False` si el `id` no existe (mismo
    patrón que `DBManager.marcar_tarea_completada`). Rechaza `estado` fuera del
    conjunto permitido.
  - `obtener(id) -> dict | None`.
  - `listar(ticket=None) -> list[dict]`.
- `entrada` / `resultado`: el repositorio acepta un `dict` (lo serializa con
  `json.dumps`) **o** un `str` ya serializado. **No importa las dataclasses de
  TF-0021** — trabaja con JSON genérico (condición de aprobación).
- Usa `get_connection()` tal cual (respeta `PRAGMA foreign_keys = ON`). Mantiene
  el estilo "una conexión por operación"; **no** introduce sesión / unit-of-work.
- El conjunto de estados `{EN_CURSO, COMPLETADA, FALLIDA}` se define como
  constantes del módulo.

## Archivos que probablemente tocará

| Archivo | Cambio previsto |
|---|---|
| `src/database.py` | `crear_tablas()`: + 1 `CREATE TABLE IF NOT EXISTS acciones` |
| `src/repositorios/__init__.py`, `src/repositorios/acciones.py` | nuevos |
| `tests/test_acciones.py` | nuevo |
| `tests/test_database.py` | **posible** añadido *aditivo*: aserción de que `crear_tablas()` crea también `acciones` (sin tocar los casos existentes) |

Objetivo explícito: **no tocar** `conftest.py` (la fixture `db` ya recrea el
esquema por test vía `crear_tablas()`), ni la lógica de los tests existentes.

**`pytest.ini`: mismo criterio que TF-0021 — no modificar automáticamente; ver
sección específica.**

## Criterios de aceptación verificables

1. Tras `DBManager()` / `crear_tablas()`, existe la tabla `acciones` con las
   columnas especificadas (`PRAGMA table_info(acciones)`); reinicializar no falla
   ni duplica (mismo patrón que `test_reinicializar_no_duplica_ni_falla`).
2. `registrar(...)` devuelve un `id` entero; la fila queda en `EN_CURSO` con
   `creado_en` no nulo y `actualizado_en` nulo.
3. `marcar(id, "COMPLETADA", resultado={...})` actualiza `estado` y `resultado`
   (legible con `json.loads`), fija `actualizado_en` y devuelve `True`; con `id`
   inexistente devuelve `False` y no crea filas.
4. `marcar(id, "ESTADO_RARO")` es rechazado (excepción o `False` documentado), sin
   escribir.
5. `obtener(id)` devuelve un `dict` con los campos deserializables;
   `obtener(inexistente)` → `None`.
6. `listar()` devuelve todas; `listar(ticket="TF-0021")` filtra por ticket.
7. `entrada` / `resultado` aceptan `dict` (se guarda JSON) y `str` (se guarda tal
   cual); round-trip `dict → guardar → obtener → dict` estable.
8. Insertar una acción con un `ticket` arbitrario **no** requiere filas en
   `tareas` / `proyectos` y **no** dispara error de FK.
9. `src/repositorios/acciones.py` no importa Flask ni `app`; `DBManager` no
   importa `RepositorioAcciones` (sin dependencia circular).
10. `python -m pytest`: **177 passed** + los nuevos; ningún test existente
    alterado en su lógica. Cobertura 100 % en los módulos tocados / nuevos.

## Dependencias

- **TF-0021** — la forma de `SalidaAgente` informa el contenido de la columna
  `resultado`; aunque el repositorio no importe la dataclass, el diseño de la
  tabla se valida contra ese JSON.
- **TF-0019** — débil: la ruta de la base ya viene de `config`.
- No depende de TF-0020.

## Riesgos

- **R1 (medio)** — `test_database.py` tiene helpers que enumeran las tablas
  esperadas; añadir `acciones` puede requerir extender un helper compartido.
  *Mitigación:* añadir casos nuevos, no alterar los existentes; si un helper es
  rígido, extenderlo de forma **aditiva**.
- **R2 (bajo)** — Formato de fecha: usar el mismo que `crear_tarea`
  (`"%Y-%m-%d %H:%M:%S"`) o ISO 8601 — decidir en el checkpoint; cubrir con test
  de formato.
- **R3 (bajo)** — Semántica de `estado`: fijar `{EN_CURSO, COMPLETADA, FALLIDA}`
  como constantes; `marcar` valida y rechaza otros valores.
- **R4 (bajo, fuera de alcance)** — Concurrencia: si en el futuro un agente en
  hilo escribe mientras la web lee, SQLite sin WAL puede dar `database is
  locked`. **No se resuelve aquí** (no hay ejecución concurrente todavía); se
  anota como hallazgo para el ticket de ejecución en segundo plano.
- **R5 (bajo)** — Tentación de añadir índices, vistas o una tabla `agentes`. **No:
  una tabla, sin índices** hasta que haya volumen real (§30).

## Qué NO debe modificarse

- El esquema de `proyectos` y `tareas`, sus FK, ni `get_connection()` (se usa tal
  cual).
- El patrón "una conexión por operación" de `DBManager`; **no** introducir capa de
  sesión / unit-of-work.
- Rutas Flask, plantillas, `app.py`.
- `conftest.py`, la lógica de los tests existentes.
- `pytest.ini` (salvo el `--cov` aditivo descrito abajo, y solo previa revisión).
- `Dockerfile`, CI, `requirements*.txt` — SQLite y `json` de la biblioteca
  estándar bastan; **cero dependencias nuevas**.
- **No crear** el runner ni ningún agente que escriba en `acciones`. En este
  ticket la tabla y el repositorio existen y se prueban **de forma aislada**.

## `pytest.ini` — análisis de cobertura (no modificar automáticamente)

Mismo criterio que TF-0021. `--cov=src` es una ruta de directorio y `coverage`
mide recursivamente; lo más probable es que `src/repositorios/acciones.py`
aparezca en el informe `term-missing` automáticamente al importarlo un test.

- **Si aparece:** no se toca `pytest.ini`; se lleva al 100 % con sus tests.
- **Si NO aparece:** el cambio mínimo sería añadir `--cov=src/repositorios` a
  `addopts` — **una línea**, documentada y sometida a revisión antes de aplicarse.
  Nunca automático.

## Estrategia de pruebas

**A ejecutar en la implementación:**

- `tests/test_acciones.py` (nuevo; usa la fixture `db` o una propia sobre SQLite
  temporal):
  - creación de la tabla + reinicialización idempotente;
  - `registrar` → estado `EN_CURSO`, `creado_en` no nulo, `actualizado_en` nulo;
  - `marcar` OK / `id` inexistente → `False` / estado inválido → rechazado;
  - `obtener` / `listar` / `listar(ticket=...)`;
  - round-trip JSON de `entrada` / `resultado` con `dict` y con `str`;
  - ausencia de restricción FK sobre `ticket`.
- Añadido *aditivo* en `test_database.py`: `crear_tablas()` crea también
  `acciones` (sin tocar los casos actuales).
- `python -m pytest` completo → **177 passed** + nuevos.
- Reporte explícito del comportamiento de `--cov=src` con el módulo nuevo.

**Manual:** ninguno — no hay entrypoint.

## ¿Requiere aprobación explícita antes de implementar?

**Sí.** Es un cambio de esquema de la base de datos y define un contrato de datos
nuevo y persistente (familia del `CLAUDE.md` §31: cambios estructurales / modelo
de datos).

Diseño aprobado en principio (2026-08-29). Antes de implementar, checkpoint sobre:

- nombre y columnas de la tabla;
- conjunto de valores de `estado`;
- formato de fecha (`"%Y-%m-%d %H:%M:%S"` vs ISO 8601);
- API de `RepositorioAcciones` (`registrar` / `marcar` / `obtener` / `listar`).

## Notas de aprobación (2026-08-29)

- `RepositorioAcciones` trabaja con **JSON genérico** y **no** importa las
  dataclasses de TF-0021.
- La tabla `acciones` es **infraestructura de trazabilidad**, no parte del
  dominio de tareas.
- `pytest.ini`: no modificar automáticamente; documentar y revisar.

## Commit

Pendiente. Sin commit ni push (documento de planificación).
