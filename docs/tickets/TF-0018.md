# TF-0018 — `README.md` de arranque (BL-05)

## Estado

NEEDS_REVIEW (implementado y verificado en local: suite en verde con Python
3.8.10, `docker build` + contenedor con `GET /` → HTTP 200 con Python 3.12.14.
Run real de GitHub Actions pendiente hasta el push. Commit pendiente de
autorización de Pao).

## Tipo

DOCS

## Prioridad

P2

## Objetivo

Crear el `README.md` de la raíz del repositorio, en español, con el arranque
local (venv) y con Docker, las variables de entorno, la ejecución de tests + CI,
el badge de GitHub Actions y un resumen de las funcionalidades y rutas actuales.
Complementa `CLAUDE.md` §29.

## Diagnóstico

Promoción de **BL-05** (`OPEN`, origen: análisis "siguiente ticket"). Antes de
este ticket **no existía ningún `README*`** en el repo (verificado:
`ls README*` sin coincidencias; `grep -i readme` solo aparecía en `docs/` como
pendiente).

Todas las dependencias documentales estaban ya satisfechas y `DONE`:

- **TF-0011** (runtime Python 3.12 en imagen y CI) y **TF-0012** (contrato de
  despliegue dev/prod, `TASKFLOW_SECRET_KEY` / `TASKFLOW_ENV` /
  `TASKFLOW_COOKIE_SECURE`) exigían "preceder a BL-05".
- **TF-0017** congeló la forma final de arranque local (`TASKFLOW_HOST` /
  `TASKFLOW_PORT` / `TASKFLOW_DEBUG`) y dejó las 7 variables documentadas en
  `.env.example`.
- **TF-0010** aportó el workflow de CI para el badge.
- **BL-04** (`DONE` vía TF-0013–TF-0016) aportó el CRUD para la sección de
  funcionalidades.

**No depende de BL-06 (WSGI):** el README *documenta* que BL-06 sigue pendiente.
Si BL-06 se implementa después, habrá que actualizar las secciones de
arranque/Docker del README — acoplamiento de documentación, no bloqueo.

## Cambios realizados

- **`README.md`** (nuevo, raíz). Secciones:
  - Título + badge de CI (`Paoox/taskflow`, workflow `ci.yml`) + descripción de
    una línea (de `CLAUDE.md` §1).
  - **Estado actual**: prototipo en evolución, MVP con CRUD de tareas; diagrama
    `Docker → Flask → SQLite`.
  - **Stack**: tabla (Python/Flask 3, Jinja2, SQLite, HTML/CSS/JS, Docker) con
    enlace a `requirements*.txt`.
  - **Estructura del repositorio**: árbol resumido comentado.
  - **Requisitos**: distinción explícita — Docker/CI verificado en **Python
    3.12**; venv local verificado en **Python 3.8.10**; sin declarar un rango
    soportado.
  - **Arranque local (venv)**: crear/activar venv, `pip install -r
    requirements.txt`, `python app.py` → `http://127.0.0.1:5000`; tabla de
    `TASKFLOW_HOST` / `TASKFLOW_PORT` / `TASKFLOW_DEBUG` con defaults; alternativa
    `FLASK_APP=app.py flask run`; nota de creación automática de `tareas.db` +
    proyecto semilla `id=0` y de que está en `.gitignore`.
  - **Arranque con Docker**: `docker build -t taskflow .`, `docker run -p
    5000:5000 -v taskflow_data:/app/data taskflow`; explicación del volumen
    `/app/data` y de `TASKFLOW_DB=/app/data/tareas.db` fijado en la imagen; aviso
    de que usa el **servidor de desarrollo de Flask** y de que el WSGI de
    producción está pendiente (BL-06); resumen del contrato neutral/producción
    del `Dockerfile` (TF-0012).
  - **Variables de entorno**: tabla de las 7 de `.env.example` (nombre / default
    / efecto), sin valores sensibles, remitiendo a `.env.example`.
  - **Tests y CI**: `pip install -r requirements.txt -r requirements-dev.txt`,
    `python -m pytest`; cobertura informativa (sin umbral); CI en cada
    `push`/`pull_request` a `main` con Python 3.12.
  - **Funcionalidades y rutas**: tabla de las 5 rutas de `app.py`; CSRF en todo
    POST; validación server-side.
  - **Modelo de trabajo**: enlaces a `CLAUDE.md`, `docs/BACKLOG.md`,
    `docs/tickets/`.
- **`.dockerignore`** — `+README.md` (1 línea, en la sección "Documentacion no
  necesaria en runtime", junto a `CLAUDE.md` / `docs/`). Mismo criterio que
  BL-11: fuera del contexto de build y de la imagen.
- **`docs/BACKLOG.md`** — `BL-05`: Estado `OPEN` → `PROMOTED`, Ticket `—` →
  `TF-0018`; párrafo "Promovido a **TF-0018**" en el detalle.

**Sin cambios** en código (`app.py`, `src/**`), `tests/**`, `conftest.py`,
`Dockerfile`, `.github/workflows/ci.yml`, `.env.example`, `pytest.ini`,
`requirements*.txt`, `CLAUDE.md`, plantillas, `static/`. Sin tocar otros BL ni el
orden posterior del backlog. Sin dependencias nuevas.

## Archivos afectados

| Archivo | Cambio |
|---|---|
| `README.md` | nuevo (raíz) |
| `.dockerignore` | +1 línea (`README.md`) |
| `docs/BACKLOG.md` | `BL-05` → `PROMOTED` / `TF-0018` + detalle |
| `docs/tickets/TF-0018.md` | nuevo |

## Pruebas

### Suite — VERIFICADO (Python 3.8.10 local, `./venv/bin/python -m pytest`)

| Verificación | Resultado |
|---|---|
| `python -m pytest` | **177 passed** (sin cambios; no hay código nuevo) |
| Cobertura TOTAL | **100 %** (`app.py`, `src/*` a 100 %) |
| `git diff --check` | limpio |
| `tareas.db` (md5) | `294cd7400314a4846667cff80c14cd95` — sin cambios |

### Docker — VERIFICADO (daemon local, `DOCKER_BUILDKIT=0`)

Ejecutado por el cambio en `.dockerignore` (R2 / CA-13):

| Verificación | Resultado |
|---|---|
| `docker build -t taskflow:tf0018 .` | PASS (`Successfully built` / `Successfully tagged`) |
| `docker run -d -p 5001:5000 -v <vol>:/app/data taskflow:tf0018` | contenedor UP |
| `curl -s -o /dev/null -w "%{http_code}" http://localhost:5001/` | **200** |
| `python --version` dentro de la imagen | `Python 3.12.14` |
| `/app/README.md` dentro de la imagen | **no existe** (excluido por `.dockerignore`) |
| `CMD` en ejecución | `flask run` (`Serving Flask app 'app.py'`, `Debug mode: off`) — sin cambios |

Contenedor, volumen e imagen de prueba eliminados tras la verificación.

### Pendiente

- **GitHub Actions**: run real del workflow sobre el commit de TF-0018 (no
  verificable sin push). CI solo ejecuta `python -m pytest`, sin cambios.

## Criterios de aceptación

| ID | Criterio | Estado |
|---|---|---|
| CA-01 | Existe `README.md` en la raíz, en español. | **cumplido** |
| CA-02 | Documenta el arranque local con venv: creación/activación, `pip install -r requirements.txt`, `python app.py` (con `TASKFLOW_HOST`/`PORT`/`DEBUG` y defaults `127.0.0.1:5000` / debug off) y `FLASK_APP=app.py flask run`. | **cumplido** |
| CA-03 | Documenta el arranque con Docker: `docker build`, `docker run` con `-p 5000:5000` y `-v taskflow_data:/app/data`, y el papel de `TASKFLOW_DB=/app/data/tareas.db`. | **cumplido** |
| CA-04 | Tabla de las 7 variables de `.env.example` (nombre / default / efecto), sin valores sensibles, remitiendo a `.env.example`. | **cumplido** |
| CA-05 | Documenta `pip install -r requirements-dev.txt` + `python -m pytest`, cobertura informativa y CI en push/PR a `main`. | **cumplido** |
| CA-06 | Badge de GitHub Actions del workflow `ci.yml` del repo `Paoox/taskflow`, enlazado a Actions. | **cumplido** |
| CA-07 | Resume las funcionalidades actuales (crear / listar pendientes / completar / editar / eliminar; proyectos de solo lectura) con tabla de rutas; sin funcionalidad no implementada. | **cumplido** |
| CA-08 | Indica que el contenedor usa el servidor de desarrollo de Flask y que el WSGI de producción está pendiente (BL-06). | **cumplido** |
| CA-09 | Enlaza a `CLAUDE.md`, `docs/BACKLOG.md` y `docs/tickets/` sin duplicar su contenido. | **cumplido** |
| CA-10 | Sin afirmaciones sin respaldo: sin licencia, demo/URL, screenshots, `docker-compose`, `CHANGELOG` ni contacto personal. | **cumplido** |
| CA-11 | `docs/BACKLOG.md`: `BL-05` promovido con enlace a `TF-0018`. `docs/tickets/TF-0018.md` creado (§29.1). Sin tocar otros BL ni el orden posterior. | **cumplido** |
| CA-12 | Sin cambios de código: `177 passed` / cobertura 100 %; `git diff --check` limpio; `tareas.db` intacta. | **cumplido** |
| CA-13 | `README.md` añadido a `.dockerignore`; `docker build` OK y `GET /` = 200; `README.md` no entra en la imagen. | **cumplido** |

## Riesgos y decisiones

- **R1 (bajo) — Documentación que envejece.** No se fijan cifras duras en el
  `README.md` (nº de tests, % de cobertura); el badge de CI y `requirements*.txt`
  son la fuente viva.
- **R2 (resuelto) — `README.md` en la imagen Docker.** `.dockerignore` no
  excluía `*.md` global. Se añade `README.md` explícitamente; verificado que no
  entra en la imagen y que el `build` sigue OK.
- **R3 (nulo) — Impacto en comportamiento.** Ticket DOCS; el único cambio
  no-documental (`.dockerignore`) solo afecta al contexto de build, no al
  runtime. Suite, app y `CMD` del contenedor sin cambios.
- **R4 (bajo, decidido) — Versión de Python.** El `README.md` distingue: Docker/CI
  verificado en 3.12; venv local verificado en 3.8.10. **No** se afirma soporte
  oficial de un rango 3.8–3.12.
- **R5 (decidido) — Sin datos personales.** Atribución vía repo `Paoox/taskflow`
  y `CLAUDE.md`; sin email ni contacto.
- **Decisión — Idioma español**, coherente con `CLAUDE.md` y `docs/`.
- **Decisión — Sin sección de despliegue de producción** más allá de citar el
  contrato del `Dockerfile` (TF-0012) y marcar BL-06 como pendiente.
- **Decisión — Ticket no divisible**: entregable único (`README.md`).

## Dependencias

- **Depende de** (todas `DONE`): TF-0010 (CI + badge), TF-0011 (runtime 3.12),
  TF-0012 (contrato dev/prod), TF-0017 (forma final de arranque local),
  TF-0013–TF-0016 / BL-04 (funcionalidades).
- **NO depende de** BL-06 (WSGI): el README lo documenta como pendiente.
- Con TF-0018, **BL-05** queda `PROMOTED` (→ `DONE` tras el run de CI en el push).

## Orden posterior del backlog

`OPEN` restante tras TF-0018: **BL-06** (única hoja del backlog).

```
TF-0019  BL-06   WSGI de producción (gunicorn)   cuando exista objetivo real de despliegue (CLAUDE.md §30 / §25.8)
```

Sin cambios respecto al orden propuesto en TF-0016 / TF-0017.

## Hallazgos nuevos

Ninguno que justifique un BL. Observaciones (no defectos, no BL):

- La nota de TF-0011 sobre recrear el venv local en Python 3.12 sigue pendiente;
  no forma parte de BL-05 y no se crea BL sin decisión explícita.
- `.dockerignore` excluye ahora `CLAUDE.md`, `docs/` y `README.md` uno a uno; si
  en el futuro se añaden más `*.md` a la raíz podría valorarse un patrón `*.md`
  (con excepciones) — mejora menor, no se registra BL.

## Commit

Pendiente. No se ha hecho commit ni push (a la espera de revisión del reporte
pre-commit y autorización de Pao).
