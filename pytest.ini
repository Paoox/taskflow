[pytest]
# TF-0005 — Configuración mínima de la suite de pruebas.
# TF-0010 — cobertura informativa (sin umbral que rompa el build).
testpaths = tests
pythonpath = .
addopts = --cov=src --cov=app --cov-report=term-missing
